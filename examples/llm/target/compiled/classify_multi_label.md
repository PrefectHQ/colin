# Multi-Label Classify Example

This document demonstrates how to use the `classify()` filter with `multi=True` to categorize content into multiple labels.

## Source Content

# Product Feedback

"I love the new interface! It's so much more intuitive than before. The dark mode is perfect for late-night coding sessions. However, the app crashes frequently when I try to export large files, which is really frustrating. Great features overall, but the pricing is too high for what you get."

---

## Multi-Label Classification Result

**Topics**: ['bugs', 'features', 'interface', 'performance', 'pricing']