---
title: Welcome to Colin!
sidebarTitle: Welcome!
description: A context engine that keeps agent skills fresh.
icon: hand-wave
---
<img 
  src="/assets/logos/c-watercolors.png" 
  
  alt="'C' logo on a watercolor background"
  noZoom
  className="rounded-2xl block"
  />

**Colin keeps agent skills fresh.** Write templates that reference live sources like GitHub files, MCP servers, and other documents. Colin compiles them, tracks dependencies, and rebuilds only what's stale.

<CodeGroup>

```markdown Template
---
name: deployment-process
description: How to deploy code to staging and production
---

# Deployment

{{ colin.github.file('acme/platform', 'docs/DEPLOY.md').content }}

## Environments

{{ ref('infrastructure/environments').content }}

## Recent Incidents

{{ colin.mcp.pagerduty.resource('incidents?team=platform&days=30') | llm_extract('deployment-related issues and how they were resolved') }}
```

```markdown Output
---
name: deployment-process
description: How to deploy code to staging and production
---

# Deployment

All changes ship through CI. Push to main, wait for checks, then promote:

1. Open a PR against `main`
2. CI runs tests, linting, and builds a preview
3. Get approval from a code owner
4. Merge—this deploys to staging automatically
5. After verification, run `workflow_dispatch` on the deploy action to promote to production

## Environments

| Environment | URL | Deployment |
|------------|-----|------------|
| Staging | staging.acme.dev | Auto on merge to main |
| Production | acme.com | Manual promotion |

## Recent Incidents

- **2025-01-15**: Database migration failed during deploy. Root cause: migration script assumed empty table. Fixed by adding existence check.
- **2025-01-08**: Staging deploy stuck in pending. Root cause: GitHub Actions runner quota. Fixed by switching to larger runner pool.
```

</CodeGroup>

Run `colin run`. Colin fetches the deployment guide from GitHub, resolves the reference to your environments doc, extracts incident summaries through an LLM, and writes the compiled skill. Run it again tomorrow—if nothing changed upstream, nothing rebuilds. If the deployment guide in GitHub was updated, Colin detects the new commit and recompiles.

## Context as Code

When Colin compiles a document, it records what sources were used and their versions—commit SHAs for GitHub files, timestamps for HTTP resources, version identifiers for MCP resources. This information is stored in a manifest alongside the compiled output.

On subsequent runs, Colin checks the manifest against current source versions. Documents whose sources haven't changed skip compilation entirely. When a source does change, Colin recompiles the affected document and any documents that depend on it.

LLM calls are cached separately by input hash. If a document recompiles but the input to an LLM block is unchanged, the cached LLM response is reused.

## How It Works

**References**: `ref('other-doc')` fetches content from another document and registers a dependency. When `other-doc` changes, documents that reference it recompile. See [References](/projects/references).

**Providers**: Built-in providers connect to external sources: [GitHub](/providers/github) repositories, [HTTP](/providers/http) endpoints, [S3](/providers/s3) buckets, and [MCP servers](/providers/mcp). Each provider tracks source versions for staleness detection.

**LLM Processing**: The `| llm_extract()` filter and `{% llm %}` blocks send content to language models. See [LLM Provider](/providers/llm) for the full API.

**Manifests**: The manifest records what was compiled, from what sources, at what versions. This powers incremental builds and enables `colin run --update` from any location.

## Beyond Skills

Colin is a general-purpose context engine. Skills are the most common output because they're how agents like Claude Code, Codex, and Cursor consume context autonomously—but Colin compiles any document that needs to stay current.

Change the output target to write directly to Claude Code's skills folder:

```toml colin.toml
[project.output]
target = "claude-skill"
scope = "user"
```

Or set `target = "local"` and `path = "dist/"` to write to a directory. The compilation model is the same regardless of where the output goes. See [Configuration](/running/configuration) for all output options.

## About the Name

Colin stands for **Co**ntext **Lin**eage. It's also a nod to [Colin the robot](https://hitchhikers.fandom.com/wiki/Colin) from the Hitchhiker's Guide to the Galaxy—a security robot who feels genuinely delighted whenever he's being helpful.

## Get Started

[Install Colin](/getting-started/installation), then work through the [Quickstart](/getting-started/quickstart) to compile your first skill.

Colin is made with 💙 by [Prefect](https://prefect.io).
